services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: cinema
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - cinema_pg:/var/lib/postgresql/data

volumes:
  cinema_pg:

